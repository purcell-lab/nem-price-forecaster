# NEM Price Forecaster sidecar dependencies
#
# darts[notorch] = the tabular/statistical model subset only — avoids PyTorch (~2 GB)
# and still provides LightGBMModel which is the only Darts model used here.
numpy>=1.26,<3
scikit-learn>=1.4,<2
lightgbm>=4.0,<5
u8darts[notorch]>=0.29,<1
fastapi>=0.111,<1
uvicorn[standard]>=0.29,<1
apscheduler>=3.10,<4
aiohttp>=3.9,<4
pydantic>=2.6,<3
